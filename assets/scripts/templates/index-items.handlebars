<table class="table table-hover table-responsive">
  <caption>You may use any item, but only edit your own.</caption>
  <tr>
    <th>
      Item Name
    </th>
    <th>
      Unit of Measure
    </th>
    <th>
      Quantity per unit
    </th>
    <th>
    </th>
  </tr>

  {{#each items as |item|}}
    <tr data-item="{{item.id}}">
      <td>
        {{item.name}}
      </td>
      <td>
        {{item.unit}}
      </td>
      <td>
        {{item.unit_quantity}}
      </td>
      <td>
        <button title="Add to a list" data-item="{{item.id}}" class="btn-sm btn-warning">&#43;</button>
        {{#if item.owner}}<button title="Edit item" data-edit-item="{{item.id}}" class="btn-sm btn-primary">Edit</button>{{/if}}
      </td>
    </tr>
    <tr class="hidden" data-itemhide data-item="{{item.id}}"><td colspan="4" data-item="{{item.id}}"><td>
    </tr>
  {{/each}}
</table>
